### Description
Login and registration forms wired to backend JWT.

### Checklist
- [ ] Complete Login.tsx form -> POST /auth/login
- [ ] New Register.tsx page
- [ ] `useAuth` hook (token storage, auto-refresh, Axios interceptor)
- [ ] Route guards (redirect to /login when unauthenticated)
